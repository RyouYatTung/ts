"use strict";
class Sorter {
    constructor(collection) {
        this.collection = collection;
        this.collection = collection;
    }
    sort() { }
}
const sorter = new Sorter([20, 3, 5, 6, -4]);
sorter.sort();
